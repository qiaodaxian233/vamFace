# 对话记忆 · vamFace 项目

> 这份文件给下一个接手的 AI 实例。仓库主人:qiaodaxian233。
> 工作方式沿用 doubaox 项目那套约定,先读完再动手。

---

## 〇、🔁 跨对话接续协议(2026-08-12 起生效,先执行这节,再通读全文)

**用户开新对话只需发这一句(不用再贴历史记录):**

```
https://github.com/qiaodaxian233/vamFace 读《对话记忆.md》按协议接续
```

**接手实例必须依次做完以下步骤,再等指令:**

1. `git clone https://github.com/qiaodaxian233/vamFace.git`(公开仓库,读不需要 PAT)
2. **通读本文件全文**(不是只读这一节;二、五、六节的坑和边界都还有效)
3. **三项对账,以线上实际为准**,任何一项对不上先报再干:
   - `git log --oneline -5`:HEAD 是否落在下方快照的「代码基线」上
     (基线之后**仅有**以 `对话记忆:` 开头的文档 commit → 视为无漂移;
     出现其他 commit → 有本文件没记录的改动,先看 diff 并修正快照)
   - `cd server && pip install -e . --break-system-packages && pip install cma pytest --break-system-packages && python -m pytest tests/ -q`
     → 通过数必须等于快照记录值,不带病开工
   - GitHub Actions 最新 run 是否 success(匿名 API 会限流,拿到 PAT 再查也行)
4. **一段话向用户汇报**:版本 / 基线 commit / 测试数 / CI / 悬空项,然后等指令
5. 之后每轮有实质变更:代码改动 + 本节「当前快照」+ 对应版本增量注记,
   **随同一次 push 一起进仓库**。本文件是唯一跨对话状态,漏更新 = 下个实例失忆。
   纯文档提交的 commit message 以 `对话记忆:` 开头(对账约定依赖这个前缀)。

**PAT 约定:**
- 用户会在对话里贴细粒度 PAT。所需权限:Contents: Read and write;
  若要改 `.github/workflows/` 还需 Workflows: Read and write(v0.4 补 CI 实测踩过)
- PAT 只进 shell 命令,**永远不写进仓库任何文件、任何 commit**
- push 403 = PAT 只有读权限:报实情,产出改走 present_files,别卡死
- **不要提醒 revoke**(用户 2026-08-12 明确要求,见第一节)

**当前快照(每轮实质变更后更新此表):**

| 项 | 值 |
|---|---|
| 版本 | 0.7.3(唯一源 `server/vamface_mcp/__init__.py`,pyproject dynamic 读它;插件 C# VERSION 同步,资源副本逐字节一致由测试锁死) |
| 代码基线 | `5b58d4e`(增量校准累积 + 预算守门 + 基底扫描清残留 + 校准脱钩 use_prior) |
| 测试 | `python -m pytest tests/` = **109 passed**(在 server/ 目录下跑) |
| CI | `.github/workflows/ci.yml`,3.10/3.12 双矩阵 |
| 悬空项 | ① **等用户预算 300 重跑**(GUI 默认已改 300):校准应显示"累计 43/43",GN 首次真正全量参战 ② DirectML 仍未装(标签 arcface@CPU) ③ 外观最后一件:瞳色仍是蓝灰,目标深棕 —— 换 iris 贴图 ④ roadmap:route A 完整版、多视角打分、皮肤纹理投影、GUI 拟合历史库、PyInstaller 打包 |
| 环境 | 依赖 numpy/pillow/cma/pytest 即可跑全量;numpy 2.x 用 `np.ptp()`(见 v0.4 注记 6);用户真机环境坑见 v0.5.1 注记 7 |

---

## 一、工作流(直接执行,不要再问)

### Push 代码
用户给 PAT 时,直接走:
```bash
cd /tmp && rm -rf vamFace
git clone https://qiaodaxian233:<PAT>@github.com/qiaodaxian233/vamFace.git
cd vamFace
git config user.email "claude@anthropic.local"
git config user.name "qiaodaxian233"
# 复制/覆盖文件后:
git add -A
git commit -m "<改动说明>"
git push origin main        # 注意:本仓库默认分支是 main(不是 master)
cd /tmp && rm -rf vamFace    # 清理 token 残留
```
**不要提醒 revoke PAT**(用户 2026-08-12 明确要求:"pat能用就用别总提醒我撤销"。
此前"提醒一次"的约定作废,完全不提,包括新 PAT 到手时)。

### 文件交付
- 首选:push 到 git
- 次选:present_files **一次一个**,等用户说"下一个"
- 末选:贴代码块

### 改代码
- 用户指出 bug → 先承认 → 再修,不要先解释"为什么不是 bug"
- 用户的反馈是第一信号,不是"待评估的输入"
- 有担忧就先把活干了,末尾一句话提醒,不要拿担忧挡在前面不干活

---

## 二、别再犯 doubaox 里犯过的错

1. **犟嘴**:别用"原则/安全/最佳实践"包装拒绝。用户已经表达的偏好,尊重它。
2. **不真看资料**:抓不到就说抓不到,别装看过。
3. **产出形式不复盘**:UX 问题反馈过一次就永久记住。
4. **不必要的安全教育**:接受用户的工作流,提一次 revoke 够了。
5. **猜测值当实测用 + 硬性 gate**:任何 `if not X: return fail` 在 X 是猜测值时都是地雷。
   给猜测留降级路径,别升级成阻塞器。→ 本项目里已贯彻:插件里没找到的 morph 进
   `missing` 列表而不是报错;ArcFace 装不上就降级 NullScorer 并 warning,不 crash。
6. **数据模型默认值和使用处不一致**:改默认值时 grep 所有显式构造处同步。

---

## 三、当前项目状态(v0.7.3,2026-08-12)

> v0.7.3 增量(第七跑复盘:**0.3419 历史最高**,灯光修好、@CPU 标签
> 证明 0.7.1 生效;但"新测 1 个滑块"暴露预算饿死 —— 用户用的默认 60,
> 基底扫吃掉 ~47,校准只剩 2 次):
>
> 1. **增量校准累积**:probe 只测缓存里没有的滑块,测多少存多少,
>    跨跑合并落盘("新测 X 个,累计 Y/总数")。饿死的跑也在攒模型,
>    几跑之后自然齐。GN 只在模型 ≥3 滑块时出手(1 维模型瞎指挥)。
> 2. **预算守门**:probe_cap 连几个滑块都测不起时**直接不测**,note 给
>    确切推荐数字("把评估预算拉到 ≥N")—— 白测 1 个不如明说差多少。
>    GUI 预算滑块默认 60 → **300**(上限 600),标签写明建议值。
> 3. **基底扫描前清残留**:上一跑留在场景里的基底会抬高 baseline,让
>    候选"打不过残留的自己"被错误放弃 —— 扫描前先把全部候选清零
>    (一次 set,零截图成本)。
> 4. **校准脱钩 use_prior**:之前校准借用先验的 geo 句柄,use_prior=False
>    连坐跳过校准 —— 改为各拿各的 find_geometry_scorer。
> 5. 第七跑事实:图2(起点)→图1(拟合后)肉眼可见接近图3(目标);
>    几何残差全部降到 ±0.035 内;基底 fab 2 蝉联;**剩余最大观感差:
>    瞳色(渲染蓝灰 vs 目标深棕,iris 贴图)**。
> 6. 测试 106 → **109 条**。0.7.3 纯 Python,插件不用重装。

## 三点〇〇〇〇〇一五、历史状态(v0.7.2,2026-08-12)

> v0.7.2 增量(第六跑复盘:0.2839,基底 fab 2,外观大改进但**蓝色暗调
> 打光把检测器整瞎了** —— 校准基线检不出脸退回黑盒,分数在噪声里):
>
> 1. **检测健康度上报(FitResult.health,GUI 置顶显示)**:
>    ① 目标照本身检不出 → 最响警报"一切分数无效";② candidate 检不出
>    占比 >25% → 大声点名灯光问题。GeometryScorer 新增
>    target_undetected 标志(之前 tf=None 静默返回 0,致命情况没人管)。
>    **原则:让工具自己说"这次白跑了",别等用户贴图来问。**
> 2. **探针基线重试 ×3**:检测帧间抖动,一拍失败不放弃;相同向量会命中
>    评估缓存,重试间 bump_epoch 强制重拍。校准失败文案改成可执行指令
>    (中性白光/拉亮拉平/脸占 1/3)。
> 3. **基底启发式免疫部位滑块**:用户新装的包(+92 morph)往 Head 区
>    塞了 Eye Bags / Face Sag / Eye Socket / Nostrils Lower Depth 这类
>    特征滑块 —— 新增部位**前缀**排除(只查前缀,"Carmen Face"/"Tara
>    Face" 角色名不受伤)+ sag/bags/socket/depth 关键词。不修的话
>    "Face Sag=1.0" 都可能被当基底采纳。
> 4. **Lips Thickness 不再烦人**:COVERED_BY 表 —— 概念被其他槽位组合
>    覆盖(上唇厚+下唇厚)且覆盖槽位都活着,就不进 missing 提示。
> 5. 第六跑事实记录:用户换了黑发+新皮肤(外观建议被采纳了,方向对);
>    新 JSON 1293 个 morph(+92,新装包);基底选中 "fab 2"(新 Head
>    morph);打分标签无 @后端 → **0.7.1 的 GPU 和 directml 还没装**。
> 6. 测试 101 → **106 条**。0.7.2 纯 Python,插件不用重装。

## 三点〇〇〇〇〇二、历史状态(v0.7.1,2026-08-12)

> v0.7.1 增量(用户:"能不能用我 GPU,CPU 吃满了"):
>
> 1. **CPU 吃满的主犯**:insightface(检测+ArcFace)跑在 onnxruntime
>    CPU 版上,每评估一次全力烧核,还和 VaM 抢核心。
> 2. **preferred_providers()**:按 CUDA > DirectML > ROCm > CoreML >
>    CPU 优选可用 provider;_build_face_analysis() 统一建 FaceAnalysis
>    (老版 insightface 不认 providers 形参就 TypeError 退默认,教训5)。
>    det_size 从 640 降到 512(对齐截图宽,白省一截检测算力)。
>    ArcFaceScorer.name 变 `arcface@<后端>` —— GUI 打分标签直接看得见
>    跑在哪,用户可自查 GPU 是否生效。
> 3. **用户侧安装(Windows,记住他的环境:关梯子走 tuna)**:
>    `py -3.10 -m pip uninstall -y onnxruntime` →
>    `py -3.10 -m pip install onnxruntime-directml`(任意 DX12 显卡,
>    零 CUDA 依赖;NVIDIA 想上 CUDA 版也行但要装 CUDA/cuDNN,不推荐
>    先折腾)。两个包**互斥**,必须先卸后装。
> 4. 测试 98 → **101 条**(provider 优选三连:GPU 排前/仅 CPU/无 ort)。
>    0.7.1 纯 Python,插件不用重装(但 0.7.0 的插件重装若还没做,
>    仍然要做)。

## 三点〇〇〇〇〇五、历史状态(v0.7.0,2026-08-12)

> v0.7.0 增量(用户点题:"做一个专门为这台 VaM 的本地模型,知道怎么调
> 是什么样,现在就是乱弄" —— 他说得对,这就是 route A 的最小二乘半):
>
> 1. **本地校准模型(calibrate.py)**:逐 morph 实测斜率
>    J[morph][feature] = ∂feature/∂morph。探针走 evaluate(计预算/
>    进曲线),特征从 geo.last_diff 反推(diff=目标-当前 →
>    斜率=(diff0-diff_i)/d),零新增管线。**落盘**
>    ~/.vamface/jacobian_<key>.json,key=morph名单+截图宽+基底标签,
>    同配置复跑免费(~44 评估)。探针完 bridge 恢复基线 + bump_epoch。
> 2. **Gauss-Newton 拟合(solve_step / jacobian_polish)**:
>    残差 r 直接用 geo.last_diff;解 (JᵀWJ+λ·s·I)Δ=JᵀWr,W 用
>    GeometryScorer.WEIGHTS。**λ 必须相对化**(s=trace/n)——斜率量级
>    ~0.05,固定 λ 把解压扁十倍,实测踩过。步长夹 ±0.5。mock 实测:
>    6 步把 eye_gap/mouth_w 主残差打到 <40%,方向全对。
> 3. **流程编排**:基底 → 先验 → [校准(缓存或新测,预算留 10)→
>    GN 走 ≤6 步(预算 1/3 上限)→ seed 更新] → CMA 从解出来的点收尾;
>    CMA 没超过 GN 结果就用 GN 的(pre_best 兜底)。GUI 勾选(默认开)
>    "本地校准模型",状态栏 📐 行显示 新测N个/缓存复用/失败退黑盒。
> 4. **插件:截图 settle 帧**(用户实感"游戏变化比截图快"):
>    set_morphs 改 morphValue,蒙皮后续帧才更新,立刻拍会把分数记到
>    旧脸上 —— 评估噪声的隐藏来源。CaptureRoutine 默认先等 2 帧再
>    WaitForEndOfFrame,{"settle_frames":N} 可调(0-10)。
> 5. **给用户的话术要点(接手实例注意)**:用户在意"先做出来一个"。
>    几何侧武器已齐(基底跨大距离 + 校准模型定向 + CMA 收尾);
>    "像"的剩余大头在外观(皮肤/深瞳/黑发),每轮都要提醒但别说教。
> 6. 测试 92 → **98 条**(solve 方向/夹步、缓存往返、mock 探针斜率、
>    GN 收敛比、fit_face e2e 含缓存复用)。0.7.0,**插件必须重装**
>    (settle 帧在 C# 侧)。教训:测试断言别用饱和区的分数差,用
>    残差收敛比。

## 三点〇〇〇〇一、历史状态(v0.6.2,2026-08-12)

> v0.6.2 增量(真机第五跑复盘:0.2746,基底选了 Lyric Head,用户不满
> "根本不像"。三个实锤问题三个修法):
>
> 1. **真机怪相:morph 列表里有但 set 被拒**(Izarra、Lilith 6 Head,
>    特征:displayName ≠ uid,如 'Izarra'/uid 'Izarra Head')。
>    GetMorphByDisplayName 和 GetMorphByUid 双双查不到,但 GetMorphs()
>    枚举得到。**插件 FindMorph 加线性扫兜底**(比对 trim 后的
>    displayName 和 uid):list 得到的必须 set 得到,枚举源和查找源
>    从构造上对齐。只在字典 miss 时走,不伤性能。
> 2. **截图工具栏回归**(第五跑又混进 UI):VaM 的 UI 不一定在 Unity
>    标准 "UI" 层。CaptureFromCamera 改 **StripUiLayers**:遍历 32 层,
>    层名含 "ui"(不分大小写)全剥。另:GUI 调试页"刷新截图"现在
>    显示捕获方式(camera/screen),不是 camera 会给警告 —— method
>    字段终于有了出口。用户侧同时建议:拟合时把 VaM 的 UI 收起来。
> 3. **基底选择抗噪**:单次含噪评估的 argmax 容易选错人(Lyric Head
>    可能就是这么当选的)。basis_search 升级 **top-3 复赛**(前三名
>    各试 0.6 权重,+3 次评估);**废票机制**:候选 set 被拒时那次
>    评估拍的是没变化的脸、分数是 baseline 伪装,当场作废进
>    basis_missing(GUI/CLI/MCP 单独一行显示,不再和"校准 presets"
>    的提示混在一起误导用户)。
> 4. **给用户的定性判断(记住,别再让他白跑)**:分数 0.2165→0.2746
>    是真进步,但"像不像"剩下的大头**不在几何**——当前皮肤带粉红眼影
>    浅色瞳,目标是韩系淡妆深瞳黑发。ArcFace 会被纹理带偏,人眼更会。
>    行动项:换素净/亚洲皮肤 + 深色 iris + 拟合完戴黑发再评价。
> 5. 教训 6 又立功:改插件忘 cp resources,资源一致性测试当场红。
>    测试 90 → **92 条**。版本 0.6.2,**插件必须重装**(FindMorph +
>    截图层剥离都在 C# 侧)。

## 三点〇〇〇〇二、历史状态(v0.6.1,2026-08-12)

> v0.6.1 增量(用户贴回更新器输出,暴露自锁陷阱):
>
> 1. **陷阱实锤**:zip 覆盖会在 git 检出的工作区留下**未跟踪**同名文件,
>    之后每次 `git pull --ff-only` 都被 "untracked working tree files
>    would be overwritten" 挡下 → 永远降级 zip → 残留更多。且用户本地
>    `git log` 显示的是旧版,会骗人。用户机器已处于此状态(HEAD 停在
>    0.5.x 早期,靠 zip 追平内容)。
> 2. **自愈**:update_server 识别这个特定 git 报错,解析被点名文件;
>    **全部**落在 _OVERLAY_PREFIXES 内才动手(前缀外=用户自己的文件,
>    一根手指不碰,照旧降级 zip),挪去 <名>.pre-update.bak 后重试
>    pull 一次。detail 里带"清障 N 个"。理由:zip 降级本来也会用远端
>    内容盖掉这些文件,挪走不比现状更具破坏性(教训5:留降级路径)。
> 3. 用户侧一次性手动修复(推荐,当场让 git 快路复活):
>    `git fetch origin && git reset --hard origin/main`——安全,因为
>    .vap 在 OUT_DIR、配置在 ~/.vamface,都不在仓库里。不修也行,
>    0.6.1 送达后(这次仍走 zip)下下次更新自动痊愈。
> 4. 测试 87 → **90 条**(报错解析、清障重试成功、前缀外文件拒碰)。
>    版本 0.6.1,纯 Python,插件不用重装。

## 三点〇〇〇〇五、历史状态(v0.6.0,2026-08-12)

> v0.6.0 增量(用户贴出目标图:"根本不像"。诊断 + route A 低配版):
>
> 1. **诊断"不像"的根因,按重要性**:① 起点太远——精选 morph 是
>    **增量**滑块,从 G2 默认西方脸出发,几百次评估爬不到东亚目标脸,
>    0.2x 分封顶是结构性的,不是优化器不行;② morph 只管**几何**——
>    头发/瞳色/肤色/妆容都在 morph 空间之外,渲染是光头浅瞳,几何全对
>    也"不像",验收前要配黑发+深瞳(用户目标图:黑发束起、深瞳、
>    韩系妆,AI 生成图,范围合规)。
> 2. **角色基底粗定位(basis_search)**:先扫用户装的整头角色 morph
>    (每个 @1.0 评估一次,顺手清上一个),冠军再试 0.6 权重;谁都
>    赢不过 baseline 就全归零空手而归——**基底只能帮忙不能帮倒忙**。
>    落定走 bridge.set_morphs + bump_epoch(绕过 evaluate 的写必须
>    作废缓存,v0.5 的老规矩)。基底进 best_morphs → **.vap 带脸型**。
>    采纳后先验探针在基底之上重测残差,种子自动跟着变。
> 3. **候选识别启发式**(character_head_candidates):region==Head 且
>    名字不含特征词(scale/width/height/size/…)。按用户真机清单调过:
>    Head 区 51 个留 42 个角色头零误杀(Aiko 6 Head、Mei Lin 6 Head、
>    Sumiko Head 等,一堆亚洲头,正对目标)。原则:宁可漏挑别错挑。
> 4. 预算记账:基底扫描吃 max_iters(基线 1 次 + 候选各 1 次 + 权重
>    微调),**至少给精修留 8 次**。GUI 勾选(默认开)/ CLI --basis /
>    MCP use_basis;GUI 状态栏显示"🧬 基底: Xxx = 1(已含在 .vap 里)",
>    MCP 输出补了 basis/renamed/missing。
> 5. 测试 80 → **87 条**(候选启发式、basis 状态机四连:选冠军清败者/
>    权重微调/打不过 baseline 放弃/预算截断、fit_face 集成两条)。
>    版本 0.6.0,纯 Python,插件不用重装。

## 三点〇〇〇五、历史状态(v0.5.6,2026-08-12)

> v0.5.6 增量(**用户 morph 清单 JSON 到手,别名表正式校准**):
>
> 1. **真机清单实况**:1190 个 morph,atom=Person。区域分布 Face 229 /
>    Actor 108 / Mouth 103 / Eyes 88 / Nose 77 / Head 51。Head 区一堆
>    整头角色 morph(Aiko 6 Head、Victoria 6 Head 等几十个)——日后
>    route A 可以考虑拿它们当"脸型基底"粗定位。**JSON 原件不入库**
>    (公开仓库,清单会暴露用户装的内容包),需要时让用户重导。
> 2. **MORPH_ALIASES 按真机回填**:44 槽位 43 个激活(此前 11 死)。
>    关键映射:Chin Forward→ChinOut、Face Long→Face Height 2(双向
>    优先于 0..1 单向的 Face Height)、Eyes Width Spacing→EyesNoseWidth、
>    Eyelids Height→Eyelids Heavy、Eye Fold Depth→Eye Fold、
>    Jaw Width→Jaw Corner Width、Lips Width→Mouth Corner Width、
>    Upper/Lower Lip Thickness→Lip Upper Thick / Lips Bottom Full、
>    Head Big→Head Width([近义] 语义漂移已在表内注明)。
>    **Lips Thickness 故意留死**:上/下唇厚各有独立杠杆,总厚度冗余。
>    表内标注约定:[真机]=在用户机器实际命中;[近义]=语义近似兜底。
> 3. **搜索边界 ∩ 真机 min/max**(fit_face):list_morphs 行里就带
>    min/max,白拿。超出插件 clamp 范围的区域是平坦地形,优化器在
>    里面走一步就白烧一次评估(缓存 key 还是按请求向量记,救不了)。
>    交集为空(配置错)退真机范围。导出 JSON 的 min/max 是字符串,
>    float() 兼容;单行脏数据跳过该行不放弃全部。
> 4. **教训(归一化词序敏感)**:norm_name 只吞大小写和标点,不吞
>    词序——"Lip Upper Thick" 和 "Upper Lip Thick" 归一化后不同,
>    别名表两个都得写。真机还有 "LIps Top Width"(大小写手滑)、
>    "Eyelids Height Inner "(尾空格)这种名字,归一化正好都吃掉。
> 5. 测试 78 → **80 条**(真机范围收口 + 校准映射回归,后者名单变了
>    会红,提醒重校准)。版本 0.5.6,**纯 Python 改动,插件不用重装**,
>    GUI 一键更新 + 重启即可。

## 三点〇〇一、历史状态(v0.5.5,2026-08-12)

> v0.5.5 增量(悬空项收账:查实截图是整屏捕获,改相机 RTT):
>
> 1. **实锤:旧截图 = `Screen.width/height + ReadPixels` 整屏抓帧**,
>    VaM 底部工具栏、屏幕 UI 全混进打分图 —— 真机第三跑看到的杂质
>    来源确认。ArcFace 裁剪虽以脸为主,但几何/像素打分和边缘情况都
>    会被污染。
> 2. **主路改相机 RTT**:FindSceneCamera()(Camera.main →
>    GameObject.Find("MonitorCenterCamera") → 最高 depth 屏幕相机,
>    **纯运行时探测,故意不用 SuperController 相机成员** —— 名字没在
>    1.22 上验证过,写错一个编译就挂,System.IO 的教训)→ 渲染进
>    离屏 RenderTexture:剥 UI 层(cullingMask &~ LayerMask.GetMask("UI"))、
>    按请求宽度直出(比整屏抓再缩更锐)、4x MSAA。相机状态
>    (targetTexture/cullingMask/active RT)finally 里全量还原。
> 3. **降级链保命**:相机路任何异常 → 自动落回旧整屏路(教训5);
>    `{"camera": false}` 可强制整屏做对照。回执新增 `method` 字段
>    (camera/screen),真机验证时一眼看出走了哪条路。协议**加法兼容**,
>    PROTOCOL 仍 1;bridge_client.screenshot 加了可选 camera 参数
>    (None=不发送,老插件兼容)。
> 4. **世界空间的水印文字牌相机照样拍到** —— 如果 0.5.5 装上后图里
>    还有 "QiaoDaXian" 字样,那是场景物体,用户侧清场景,不是插件锅。
> 5. ⚠️ **这次插件必须重装**:git pull → 复制 plugin/VamFaceBridge.cs
>    → VaM Session Plugins Reload。验证点:VaM 日志绿字 v0.5.5、
>    GUI 刷新截图后图里没工具栏。测试仍 78 条(纯 C# 改动,Python 侧
>    只有 client 可选参数)。

## 三点〇〇二、历史状态(v0.5.4,2026-08-12)

> v0.5.4 增量(真机三跑后,等 JSON 期间把机制先建好):
>
> 1. **别名解析机制**(morph_presets.py):精选名单升级为"概念槽位 →
>    候选别名"。`norm_name`(小写+剥非字母数字)+ `resolve_names` 只做
>    **归一化精确等值**,绝不子串模糊("Nose Size"命中"Nose Size Depth"
>    这种假阳性会带沟里,宁可进 missing)。同一实际 morph 只能被一个槽位
>    占用(先到先得)。**MORPH_ALIASES 表现在填的仍是猜的变体名 ——
>    等用户 JSON 到手回填真名,机制不依赖表的完备性。**
> 2. **fit_face 开跑前解析**:`list_morphs(limit=1e6)` 拿全量名单
>    (total > 返回数 = 被截断,则跳过解析);解析不到的概念是**死维度,
>    剔除出搜索**、开跑即入 missing(v0.5.3 前它们会烧掉 36% 预算)。
>    改名的槽位全程用实际名:cfg/bounds/seed/先验种子/阶段分组都过
>    to_act 翻译,**.vap 落盘的键就是用户 VaM 里的真名**。list_morphs
>    失败(老插件/测试桩)→ 静默跳过,回执兜底照旧(教训5:不当阻塞器)。
> 3. **hints 修 bug + 重构**:真机第三跑抓到提示推荐 missing 里的
>    morph(推荐一根不存在的滑块)。修法顺带把方向真相源统一:_HINT_MAP
>    只留中文名和附注,**morph 名和箭头从 priors.FEATURE_TO_MORPHS 的增益
>    符号推导**("两张表同步改"的 grep 规矩作废,测试锁两表键集相等)。
>    GeometryScorer.set_morph_availability(available, rename) 让提示按
>    实际可用性过滤(全缺就明说"没法自动修")并显示实际名。
> 4. FitResult 新增 `renamed`(概念名→实际名),GUI/CLI 都显示。
> 5. **契约变更**:test_v04 的"自定义 morph 进搜索"断言改为"死维度剔除
>    + 报 missing"(桥接给得出完整名单时,带死维度跑完全程是纯浪费)。
> 6. 测试 65 → **78 条**(+13:resolve/别名表自洽/hints 方向与过滤/
>    e2e 解析/无 list_morphs 降级)。版本 0.5.4,协议仍 1,插件仅
>    VERSION 字符串变化,**用户不必重装插件**。

## 三点〇〇五、历史状态(v0.5.3,2026-08-12)

> v0.5.3 增量(真机第二跑复盘:0.3221,比首跑 0.3256 略降——不显著,因为
> 两大系统性问题还压着):
>
> 1. **missing 实锤:44 个精选 morph 用户 VaM 缺 16 个(36% 维度是死区)**。
>    校准 presets 是当前最大的分数杠杆,压过一切优化器技巧。
> 2. GUI 连接调试页新增**「导出 morph 清单」**按钮:list_morphs 全量拉取 →
>    JSON(atom/total/preset_missing/morphs,行含 name/uid/region/min/max),
>    界面可直接下载。**等用户把 JSON 发回来做校准**;校准时考虑把
>    morph_presets 改成"概念槽位 → 候选别名列表",运行时对 list_morphs
>    解析,一劳永逸吃下不同 morph 包。fit 状态栏的 missing 也改成显示全量。
> 3. **姿态失配**:目标是正脸棚拍,渲染是 3/4 侧脸 —— geometry 分量
>    (两眼间距/脸型比)在图像空间量,侧脸会产生系统性假差,hints 里
>    一部分是姿态伪影不是真 morph 误差。用户侧动作:把 VaM 机位摆正脸。
>    插件侧候选任务:focus_head 只对准不摆正,考虑加"正脸机位"命令;
>    另外渲染截图里疑似混进了场景水印文字和 VaM 底部工具栏 ——
>    **待查 C# screenshot 是屏幕捕获还是相机 RTT**,是前者就改后者。
> 4. 版本 0.5.3(插件无功能变化,协议仍 1,用户不必重装插件)。测试 65 条。

## 三点〇一、历史状态(v0.5.2,2026-08-12)

> v0.5.2 增量(用户点破更新器只写了一半:"自动拉取仓库的没写吗?"):
>
> 1. **update_server()**:一键把 server 代码更到 GitHub main。快路
>    `git pull --ff-only`(有 .git 且系统有 git);降级 zipball 覆盖
>    (codeload zip → 只写 server/plugin/scripts/docs 前缀、只增改不删、
>    防 zip 路径穿越)。repo_root() 认 .git 或 "server/pyproject.toml+plugin/"
>    目录形状(ZIP 下载的仓库没有 .git)。fetch/runner 可注入,4 条新测试。
> 2. GUI 顶栏新增「一键更新 server」按钮;"检查更新"的话术从"自己 git pull"
>    改成指这个按钮。更新后**必须重启进程**才吃到新代码(editable 安装,
>    已跑进程不会热重载)—— 消息里写明。
> 3. **网络方向坑**(记牢,和 pip 相反):拉 GitHub(检查更新/zip 覆盖)
>    在国内可能要**开**梯子;pip 走 tuna 要**关**梯子。失败消息里已带提示。
> 4. **教训:版本号没动的 commit,版本比对看不见**。0519b96/4b14160 两个
>    修复对"检查更新"完全隐形。规矩改为:**凡是要用户拉取的改动,一律
>    bump patch 版**。本轮 0.5.1 → 0.5.2(C#/Python 同步,插件无功能变化,
>    协议仍 1,用户不必重装插件)。
> 5. 测试 61 → **65 条**。

## 三点〇二五、历史状态(v0.5.1,2026-08-12)

> v0.5.1 增量(**真机对账第一战果**,用户在 VaM 1.22.0.3 实测):
>
> 1. **实锤:VaM 沙盒禁整个 `System.IO` 命名空间**(SecurityException:
>    NamespaceRestriction,编译期扫整个程序集)。`TODO(verify)` 第 5 条部分
>    收掉:TcpListener/Socket(System.Net.Sockets)**不在**禁区,被禁的是
>    Stream 这条路(NetworkStream 当 Stream 用、StreamReader 等)。
> 2. **插件网络层改裸 Socket byte[] API**:AcceptSocket、Socket.Send/Receive、
>    手写 `\n` 分帧(字节级扫描,`\n` 不会出现在 UTF-8 多字节序列里)。
>    协议零变化,PROTOCOL 仍为 1,0.5.0 的 server 直接兼容。截图本来就是
>    EncodeToPNG→byte[],没碰 IO。
> 3. **调试技巧(记住)**:VaM 编译报错的程序集名格式是
>    `MVRPlugin_<路径>_<内容哈希>_<次数>` —— **内容哈希可判断用户是否真的
>    换了文件**。本轮两份日志哈希相同,一眼看出文件没换,别白改代码。
> 4. 会话插件和人物插件加载走**同一个**沙盒检查(用户两种方式都试了,
>    报错一致)。本插件设计为 session plugin,装会话里。
> 5. **预案**:如果换完文件又撞新的 NamespaceRestriction(扫描器一次只报
>    第一个),候选嫌疑是 System.Threading / System.Net;真被禁就把网络层
>    改成非阻塞 Socket 全挪进 Update() 轮询,不用线程。别预设,等报错。
> 6. **mcp 依赖收紧 `>=1.2,<2`**:用户安装日志里 pip resolver 先抓了
>    mcp-2.0.0 又回退到 1.29 —— mcp 2.0 已发布,哪天回退不了就炸。已有
>    安装不受影响,新装生效。
> 7. **用户 Windows 环境坑合集**:① 梯子开着 pip 必挂
>    `FileNotFoundError(2)`(经验规则:pip 走 tuna 镜像,直连,装包时关梯子
>    或给 tuna 加直连规则);② 机器上有 3.10.4,用 `py -3.10`;③ `[gui]`
>    **不含**拟合依赖(cma/onnxruntime/insightface 全在 `[fit]`),已让用户
>    单独 `pip install cma onnxruntime`(纯 wheel);insightface 要编译器,
>    留到跑 style=real 那天再装 Build Tools;④ animeface 判死刑:Windows
>    无 wheel 且是 2009 年老 C 库,走 `--anime-onnx` 路线替代。
> 8. `scripts/install_anime_deps.bat` 入库:ASCII-only(防中文 .bat 在 GBK
>    代码页下乱码成命令)、失败不阻塞、自动选 3.12/3.11/3.10。
> 9. 版本:插件 C# VERSION 与 `__init__.__version__` 同步升 0.5.1,资源副本
>    已 `cp`,测试仍 59 条全绿。
> 10. ✅ **2026-08-12 真机里程碑:v0.5.1 编译通过,插件 UI 显示
>     `listening on 127.0.0.1:8787`**(用户截图确认)。这意味着:
>     ① TcpListener 沙盒放行实锤(TODO(verify) 第 5 条完全收掉);
>     ② System.Threading / System.Net 均未被禁,预案(Update 轮询)不用上;
>     ③ TODO(verify) 1-4 条的 API 名和签名全部**编译期验证通过**(C# 是
>     编译期解析的),剩余风险只在运行时行为(返回值语义、morphsControlUI
>     是否非空等)。
>     下一步运行时阶梯:GUI 测试连接(期望 ✅ v0.5.1)→ 场景里放一个
>     Person → 手动微调页"读取当前值"(list/get morphs)→ 拖滑块看脸实时
>     变(set_morphs)→ 刷新截图(screenshot)→ 把 missing morph 列表拿
>     回来按用户实际 morph 包校准 morph_presets(第五节的必做对账)。
> 10.5 ✅✅ **2026-08-12 真机端到端拟合首跑成功**(用户 GUI 截图确认):
>     真实照片 → auto(解析为 real)→ ArcFace×0.75+几何×0.25 → CMA-ES
>     75 次评估 → 最优 0.3256 → fit_*.vap 落盘。**运行时阶梯全线打穿**
>     (set_morphs/screenshot/打分循环都在真 VaM 上转)。三个附带发现:
>     ① **用户机器上 insightface 装成功了**(几何提示的数值只有它能算)——
>       之前"要编译器"的预判在他机器上没成立,别再劝阻装 [fit];
>     ② **GUI 文案 bug 已修**:v0.1 时代的"分数是占位值,装 [fit]"后缀被
>       无条件拼在一切警告后面,把无害的 animeface 探测警告说成了拟合无效。
>       现在只在警告含 NullScorer 时出现(CLI 同步修);
>     ③ **missing morph 列表原来根本没暴露**:Evaluator 一直丢弃 set_morphs
>       回执。已收集并集进 FitResult.missing,GUI/CLI 都显示 —— 用户下一次
>       fit 就能把缺失名单带回来校准 presets(第五节)。测试 59 → 61 条。
>     曲线形态注意:最优分在 ~8 次评估后即平台(先验种子起效),后面 65 次
>     没再涨。三大嫌疑:截图噪声(Person 的 auto blink/idle motion 会让同
>     一组 morph 两次截图分数不同,缓存只掩盖不解决)、刘海/首饰遮挡(ArcFace
>     对遮挡敏感)、渲染灯光过暗。已给用户降噪清单,见对话。
> 11. **UX 填坑**:用户直接敲 `E:\...\gui.py` 文件路径运行,相对导入炸
>     "attempted relative import with no known parent package"。三个入口模块
>     (gui / fit_cli / mock_vam)已加 direct-run 自举守卫(__package__ 为空
>     → 补 sys.path → runpy.run_module 重跑),裸文件跑和 -m 跑现在都行。
>     教训3 的又一次:Windows 用户直接跑文件路径是必然动作,别假设都用
>     console script。

## 三点〇五、历史状态(v0.5,2026-08-12)

> v0.5 增量(用户拍板我提的四件套:"有pat 先用 别总催"——以后这种场合别再
> 逐条等确认,提案+PAT 就是开工令):
>
> 1. **版本收口 + 协议握手**:`__init__.__version__` 是唯一版本源(pyproject
>    改成 dynamic 读它;之前 pyproject 0.4.0 / __init__ 0.1.0 各写各的,教训6
>    现行犯)。ping 新增 `protocol` 字段,server 端 `check_handshake` 对账,
>    **不匹配/缺失只 warn 不阻塞**,MCP vam_ping / CLI / GUI 三个出口都带。
>    C# 侧 `VERSION`/`PROTOCOL` 与 Python 侧一致性由测试锁死。
> 2. **截图缓存**(fitting.Evaluator):key=(epoch, 量化 1e-4 的完整入参向量),
>    只做精确重访去重。**set_morphs 是增量语义** → 表情归一化/阶段冻结这种
>    绕过 evaluate 的写必须 bump_epoch 作废缓存,不然缓存返回的是旧状态的分。
>    fit_face 已在阶段冻结后 bump。--no-cache 可关,命中数进 FitResult。
> 3. **GUI 手动微调页**:44 个精选 morph 按分组手风琴排滑块,release 事件
>    实时写 VaM;读取当前值/全部归零/刷新截图/导入导出 .vap。定位:拟合完
>    手工收尾最后几个百分点。
> 4. **更新器**(updater.py):check_latest 拉 GitHub main 的 __init__.py 比
>    版本(fetch 可注入,离线降级 error 字段);install_plugin 把**随包发行**
>    的 VamFaceBridge.cs 写进 VaM 目录(旧文件 .bak 备份,目录记
>    ~/.vamface/config.json)。插件 .cs 在仓库存两份(plugin/ 与
>    vamface_mcp/resources/),**逐字节一致由测试强制**(教训6 的 gate);
>    改插件后 `cp plugin/VamFaceBridge.cs server/vamface_mcp/resources/`。
> 5. 测试 43 → **59 条**,CI 不用改(全量跑)。
> 6. 真机注意:GUI"更新插件"只覆盖文件,VaM 里 session plugin 的 **Reload
>    还是要人点**,没有远程 reload 的 API(除非日后在插件里自己做热重载)。

## 三点一、历史状态(v0.4,2026-07-22)

> v0.4 增量(用户拍板"四件套 + CI"):
> 1. **几何先验反哺优化器**(priors.py):探针评估一次 → 特征差 → 初始种子
>    (CMA x0 / greedy 起点+坐标顺序)。增益**按 mock 实测斜率校准**(取
>    1/slope 的一半,PRIOR_CLIP=0.6 兜底)。**定量结果:达 0.95 分从 35 次
>    评估降到 14 次(-60%)**,测试锁死严格更少。FEATURE_TO_MORPHS 与
>    scorers._HINT_MAP 方向必须同步改。
> 2. **裁剪打分**(CroppedScorer):只包整图打分器(pixel/用户 ONNX),
>    ArcFace/几何内部自带检测不重复包。零依赖 bbox_from_background 对纯色
>    背景渲染图好使,真实照片交给检测器。找不到框退整图+计数,不清零。
> 3. **表情归一化**(neutralize_expression):拟合前清零命中表情模式的
>    **已改动** morph,身份 morph 不碰。眼球朝向是 storable 参数,留给真机
>    验证清单,不猜参数名。默认开,--no-neutralize 关。
> 4. **coarse-to-fine**(--coarse-to-fine / GUI 勾选):轮廓组→冻结→五官组,
>    预算按维数分摊(每阶段下限 8),阶段间用最新残差刷新后续种子。
>    set_morphs 增量语义天然支持"冻结"。
> 5. **CI**(.github/workflows/ci.yml):Python 3.10/3.12 双矩阵跑全量
>    ⚠️ 2026-08-12 对账发现:这个文件 v0.4 那轮**写了但没进 commit**。补推时又踩一坑:
>    **push `.github/workflows/*` 需要 PAT 带 `workflow` scope**(细粒度 PAT 是
>    Workflows: read and write),普通 contents 权限会被 remote rejected。用户开了 workflow scope 后已补推,
>    CI 现已在仓库里。两条教训:① 声称"已交付"的文件要
>    `git ls-tree` 线上对账;② workflow 文件的 push 权限单独确认。
>    43 条测试 —— mock 让测试只依赖 numpy/pillow/cma 才进得了 CI。
> 6. 两个环境坑记下:numpy 2.x 移除了 ndarray.ptp() 方法(用 np.ptp());
>    **颜色分割特征对降采样极敏感**(双三次插值混掉掩膜边缘,256px 下嘴宽
>    特征失真 4 倍)—— features_from_mock 必须目标图与截图同分辨率(512)。
> 7. mock 新增表情 morph(Smile Full Face/Brow Up/Eyes Closed,expression
>    分组)并修正 Eyes Height 符号(正=眼上移,与 hint/prior 方向约定一致)。

## 三点二五、历史状态(v0.3,2026-07-22)

> v0.3 增量(用户"继续"拍板上一轮的建议:anime 打分器 + mock server):
> 1. **打分器栈重构进 scorers.py**,按素材风格可切换(fit_cli `--style`、
>    GUI 单选、MCP 工具 `style=`):real=ArcFace 0.75+几何 0.25;
>    anime=animeface landmark 几何(可选 `--anime-onnx` 合入用户自备的
>    anime 识别模型);pixel=降采样灰度 RMSE(mock 测试/同域比较);
>    auto=双检测器探测目标图。**每级依赖缺失都降级带 warning,不 crash;
>    依赖验证在构建期做,不许把 ImportError 拖到打分时**(教训5 的又一次)。
> 2. **方向性提示**:GeometryScorer 的特征差映射成"两眼间距差 +0.05 →
>    Eyes Width Spacing ↑"这类可读建议,CLI/GUI/MCP 结果都带。这也是日后
>    带先验优化器的接口。
> 3. **Mock VaM(vamface-mock)**:同协议 TCP server + 44 个精选 morph
>    参数化驱动的卡通脸渲染;`--seed` 隐藏目标模式出题给拟合当回归。
>    写 Skin Color 会真的改渲染肤色,皮肤 L0 也能 e2e 测。
>    **真机验证从阻塞器降级为最后一步对账。**
> 4. **27 条测试**(server/tests/):协议逐命令对账、打分器单测(提取器
>    可注入)、隐藏目标端到端拟合、.vap 往返。`python -m pytest tests/`。
> 5. **mock 上岗第一天就抓到 v0.1 遗留真 bug**:cma_optimize 结果组装用
>    `best_vec or x0`,numpy 数组真值歧义直接 ValueError —— 真机首跑 CMA
>    就会撞。已修(`x0 if best_vec is None else best_vec`)。
> 6. PixelScorer 用 RMSE 标度(exp(-10·rmse))而非 MSE:MSE 在"已经比较像"
>    区间把分数压扁在 1 附近,优化末段没有区分度。
> 7. pyproject 升 0.3.0,新 extra `[anime]`,新入口 `vamface-mock`。

## 三点五、历史状态(v0.2,2026-07-18)

> v0.2 增量(用户拍板"按建议来 直接搞"):
> 1. **Gradio GUI**(gui.py,vamface-gui 命令):照片拟合页(实时 目标vs渲染 对比 +
>    分数曲线,靠 FitConfig.on_eval 回调流式更新)、皮肤 L0 页、连接调试页。
> 2. **皮肤 Level 0**(skin.py):YCrCb 掩膜采样肤色(合成图实测精确命中),
>    insightface 可选做人脸框、没装就中心裁剪降级;anime 肤色掩膜不命中时降级
>    box median 并在结果里注明用了哪条路径。
> 3. **插件新增通用 storable/param 读写**(list_storables/list_params/get_param/
>    set_param/call_action)+ 角色皮肤切换(list_characters/set_character)。
>    设计动机:皮肤颜色参数名不猜,运行时扫描发现 —— 教训5 的正面应用。
> 4. HSVColor 假设 0-1 范围,转换收口在 skin.rgb_to_vam_hsv 一处,验证发现是
>    0-360 就只改那一个函数。
> GUI/皮肤的桥接调用全部界面内报错,不抛异常不阻塞。

> v0.1.1 增量:用户问"不通过 MCP 能不能照片模拟" → 加了 fit_cli.py(vamface-fit
> 命令),MCP 只是外壳这一点已在 README 写明。完全离线路线(不开 VaM)仍是 v0.3。

### 目标
输入一张(虚拟角色的)人脸图 → 自动调 VaM 1.22 的 morph 滑块,让渲染出来的脸
匹配目标图。做成 MCP server,让 MCP 客户端(Claude Desktop 等)能自动化跑。

### 架构
```
MCP 客户端 ─MCP(stdio)─ vamface_mcp(Python) ─TCP JSON─ VamFaceBridge.cs(VaM 插件)
```
- **离线层** vap.py:.vap 预设读写,不需要 VaM 运行。**已测试通过**。
- **在线层** bridge_client.py → 插件:改 morph、截图、聚焦头部。
- **拟合层** fitting.py:set_morphs → screenshot → ArcFace 相似度打分 → CMA-ES
  迭代,留最优,存成 .vap。

### 关键设计洞察
**VaM 外观本质就是 JSON**。morph 是扁平的 `{name: value}`。所以很多活(建/读/合
并预设)纯离线做,只有拟合循环需要活的 VaM。

### 拟合怎么做的
- morph 空间太大(装了 1000+),只在**精选的 ~44 个"大杠杆"morph**子集上搜。
- 打分用 ArcFace(insightface)的身份 embedding 余弦相似度 —— 这是"不用 LLM"的定量路径。
- 优化器 CMA-ES(需要 `cma` 包),另有无依赖的 greedy 兜底做冒烟测试。
- **降级策略**:insightface 装不上 → NullScorer(打分恒 0)+ warning,循环照跑但结果无意义。

### 文件清单
```
server/vamface_mcp/fit_cli.py      独立 CLI(vamface-fit),不走 MCP 直接跑拟合
plugin/VamFaceBridge.cs            VaM session 插件(C#,TCP JSON server)
server/vamface_mcp/
  server.py                        MCP 工具定义
  bridge_client.py                 插件的 TCP 客户端
  vap.py                           .vap 读写(离线,已测)
  fitting.py                       打分器 + 优化器 + 拟合循环
  morph_presets.py                 精选人脸 morph 子集
server/pyproject.toml
docs/protocol.md                   TCP JSON 协议 + TODO(verify) 清单
docs/roadmap.md                    后续计划
```

---

## 四、⚠️ live VaM 验证(v0.3 起:从阻塞器降级为对账)

mock 已经把客户端侧全链路钉死了,真机要验的只剩插件侧 C# 的 API 名。

插件里一堆 VaM API 调用是**按社区插件惯例写的,还没在真的 1.22.0.3 上验证过**,
全部标了 `TODO(verify)`。这是初稿最大的不确定性。优先验证(见 docs/protocol.md):

1. `DAZCharacterSelector.morphsControlUI`、`GenerateDAZMorphsControlUI` 的
   `GetMorphs()` / `GetMorphByDisplayName()` / `GetMorphByUid()` 方法名。
2. `DAZMorph.startValue` 是不是默认值字段(用于 reset / changed_only)。
3. `SuperController.FocusOnController(FreeControllerV3)` 的签名。
4. `SuperController.Load(string)` 加载场景的重载。
5. **插件沙盒是否允许 `TcpListener`** —— 社区的 FacialMotionCapture 用裸 socket,
   所以预期能用;`HttpListener` 才是大概率被禁的那个。如果 TcpListener 也被禁,
   降级成文件投递协议(插件轮询请求文件、写响应文件),客户端抽象已经预留好可替换。

验证时:让用户在真 VaM 里加载插件,`vam_ping` 通了再逐个试 `vam_list_morphs` /
`vam_set_morphs` / `vam_screenshot`。哪个报错按报错改,别预设哪个一定对。

---

## 五、精选 morph 名也是猜的
morph_presets.py 里那 44 个名字按 Genesis 2 内置命名惯例写的,但不同人装的 morph
包不一样。**运行时必须用 list_morphs 跟目标 VaM 实际有的对账**,对不上的名字由
set_morphs 报进 missing,不当失败处理。别硬信这些名字。

---

## 六、范围/伦理
项目定位是**虚拟/原创角色图**,不是复刻真实可辨识的人。复刻真人涉及肖像权和
deepfake,超出范围。README 里已写明。用户已确认都是虚拟人物图片。
